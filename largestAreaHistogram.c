#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main()
{
    int A[] = {2, 1, 5, 6, 2, 3};
    int prev, next, result, max; len = sizeof(A)/sizeof(A[0]);
    
    result = len;

/*    for (int i =0; i < len; i++)
    {
        if ( a[i] == 1)
    }
*/  
    if ( len !== 1 )
    {
        prev = 0;
        next = 1;

        while(next<len)
        {
            if (prev > next)
            {
                
            }
        }
    }
    prev = 0; next = 1;

    for( int i = 0; )

    return 0;
}